{
  "name": "@nymspace/graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify:discovery": "tsx --env-file-if-exists=../../.env --conditions=react-server scripts/verify-discovery.ts"
  },
  "dependencies": {
    "@nymspace/core": "workspace:*",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5",
    "vitest": "^4.0.18",
    "tsx": "^4.20.6"
  }
}
